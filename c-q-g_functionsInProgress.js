
function replaceIrregular(object) {
  return function(
    object.forEach(item) {
    irregularPast.forEach()
    if (item.match())
  }
}



var replaceIrregular = function(verbArray) {
  var pastVerbs = verbArray.map(function(verb) {
    return verb.past;
  });


  verbArray.past.forEach(function(person) {
    if (person.match(/goed/)) {
      console.log("test works!");
    }
  });
};

replaceIrregular(conjugated);



var irregular = {
  go: ["goed", "went"];
  speak: ["spoke", ]
}
    verb.past.forEach(person) {
      if(indexOf)
    };
    verb.presentPerfect.forEach(person) {
      if(indexOf)
    };
    verb.pastPerfect.forEach(person) {
      if(indexOf)
    };
  };
  return newArray;
}
